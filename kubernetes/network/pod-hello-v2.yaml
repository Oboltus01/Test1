apiVersion: v1
kind: Pod
metadata:
  name: hello-v2
  namespace: net-demo
  labels:
    app: hello-v2
spec:
  containers:
  - name: hello
    image: ghcr.io/borisovcloud/devops2079/hellogo:latest
    ports:
    - containerPort: 8080
    env:
    - name: GREETING
      value: version 2
    resources:
      requests:
        memory: "6Mi"
        cpu: "250m"
      limits:
        memory: "32Mi"
        cpu: "500m"
    readinessProbe:
      httpGet:
        path: /
        port: 8080
      periodSeconds: 1
    livenessProbe:
      httpGet:
        path: /
        port: 8080
      periodSeconds: 1
---
apiVersion: v1
kind: Service
metadata:
  name: hello-v2-svc
  namespace: net-demo
spec:
  selector:
    app: hello-v2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-v2-ingress
  namespace: net-demo
spec:
  rules:
    - host: hello.local
      http:
        paths:
          - path: /v2
            pathType: Prefix
            backend:
              service:
                name: hello-v2-svc
                port:
                  number: 80
